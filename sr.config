[{kernel,
  [{logger,
    [{handler, all, logger_std_h,
      #{ config => #{ file => "log/sr.log",
                      max_no_bytes => 4096,
                      max_no_files => 5},
         formatter => {logger_formatter, #{}}}}]}]}].